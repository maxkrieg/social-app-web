query Event($id: ID!) {
  event(id: $id) {
    id
    title
    location
    description
    createdAt
    updatedAt
    eventUsers {
      role
      user {
        id
        username
      }
    }
  }
}
